extends layouts/main-layout.pug

block styles
  link(rel="stylesheet", href="/css/product.css")
block content
  main
    form.product-form(action="/admin/product", method="POST")
      div.form-control
        label(for="title") 标题
        input(type="text", name="title")#title
      button(tpye="submit") 提交